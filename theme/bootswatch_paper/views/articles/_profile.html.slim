- if User.first
  .panel.panel-default
    h5.panel-heading
      i.fa.fa-user
      '  Profile
    .panel-body
      = link_to image_tag(User.first.image.url(:medium)), User.first.image.url(:original), class: 'thumbnail'
      = User.first.name
